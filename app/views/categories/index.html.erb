<%= render partial: "/layouts/breadcrumbs" %>

<div class="mt-4">
  <% @categories.each do |cat| %>
    <%= link_to category_docs_path(cat) do%>
      <div
        class="flex items-strecth shadow bg-white rounded-lg mb-3 overflow-hidden p-4"
      >
        <%= cat.title%>
      </div>
    <%end%>
  <%end%>
</div>
